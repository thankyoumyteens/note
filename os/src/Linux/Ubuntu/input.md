# 中文输入法

### 1. 安装 fcitx5

```sh
sudo apt-get install fcitx5 fcitx5-configtool fcitx5-chinese-addons
```

### 2. 安装完后在开始菜单中打开 fcitx5 configuration

添加`拼音`输入法

### 3. 设置开机自启

```sh
sudo apt install gnome-tweaks
```

安装完成后在开始菜单中打开 tweaks, 把 fcitx5 添加到开机自启
