# 禁止部分代码格式化

在注释中写 `@formatter:off` 和 `@formatter:on`, 以 java 代码为例:

```java
// @formatter:off  关闭格式化

// @formatter:on   开启格式化
```

idea 中开启相应的支持: Editor -> Code Style -> Formatter Control -> Enable formatter markers in comments
