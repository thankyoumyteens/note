# 调试 jar 包(远程调试)

1. 通过终端运行 jar 包:

```sh
java -Xdebug -Xrunjdwp:transport=dt_socket,address=5005,server=y,suspend=y -jar myApp.jar
```

2. 程序会暂停等待调试连接
3. 打开 idea
4. Edit Configurations... -> + -> Remote JVM Debug
   - Host: localhost
   - Port: 5005
5. 启动调试
