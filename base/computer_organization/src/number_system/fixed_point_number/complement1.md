# 补码求负数

二进制补码数快速求负数的方法: 将包括符号位在内的每 1 位取反, 然后结果加 1。例如 -5 的补码是 11111011, 那么 +5 的补码就是 00000101。

这种方法的原理是: 一个数 x 和它按位取反的结果 x<sub>取反</sub> 相加, 和是 111...111, 即 -1。 因为 x + x<sub>取反</sub> = -1, 所以有 x + x<sub>取反</sub> + 1 = 0, 所以 x<sub>取反</sub> + 1 = -x。
