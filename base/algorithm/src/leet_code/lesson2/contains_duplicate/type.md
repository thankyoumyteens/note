# 题型识别信号

题目特征：

```text
给一个整数数组 nums
判断是否存在重复元素
如果某个值出现至少两次，返回 true
否则返回 false
```

识别信号：

```text
是否重复
是否出现过
是否存在相同元素
返回 boolean
```

看到这些信号，优先想到：

```text
HashSet
```

原因是这题只关心：

```text
某个数字以前有没有出现过
```

不关心：

```text
出现了几次
出现在哪个下标
对应什么额外信息
```

所以不需要 `HashMap`，`HashSet` 足够。
