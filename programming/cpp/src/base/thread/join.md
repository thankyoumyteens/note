# join 函数

join()函数的主要作用:

1. 等待子线程完成: 调用 join()后，当前线程会暂停执行，直到被调用的子线程执行完毕（即线程函数返回），才会继续往下一行代码。这确保了主线程不会在子线程完成工作完成前提前退出
2. 回收子线程资源: 子线程执行完毕后，其相关资源（如线程描述符、栈空间等）需要被回收。join()会负责清理这些资源，避免资源泄漏。
3. 避免程序异常终止: 若一个 std::thread 对象既未调用 join()也未调用 detach()，在其析构时会触发 std::terminate()，导致程序异常终止。join()是避免这种情况的重要手段
