# 内联函数

内联函数是一种特殊函数，建议编译器将函数体代码直接替换函数调用，从而减少调用开销。

- 普通函数调用：程序跳转到函数地址执行，需保存/恢复上下文，有开销。
- 内联函数：编译器直接将函数体插入调用处，无需跳转，提升性能。

定义方式: 在函数声明和定义前面加上 `inline` 关键字(内联函数常在头文件中定义)

```cpp
inline int add(int a, int b) {
    return a + b;
}
```

注意: 编译器可能忽略 inline（如函数复杂时）。

即使未声明 inline，现代 C++ 编译器也可能自动内联简单函数。
