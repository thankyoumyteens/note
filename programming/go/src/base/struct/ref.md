# 指针引用和值引用

将指向结构体的变量赋给另一个变量时，被称为赋值:

```go
a := b
```

赋值后，a 与 b 相同，但它是 b 的副本，而不是指向 b 的引用。修改 b 不会影响 a，反之亦然。

要修改原始结构体实例包含的值，必须使用指针。指针是指向内存地址的引用，因此使用它操作的不是结构体的副本而是其本身。要获得指针，可在变量名前加上和号。

```go
a := &b
```

修改 b 时，将修改分配给 a 的内存，因为 a 和 b 指向相同的内存。

如果需要修改原始结构体实例，就使用指针。如果要操作一个结构体，但不想修改原始结构体实例，就使用值。
