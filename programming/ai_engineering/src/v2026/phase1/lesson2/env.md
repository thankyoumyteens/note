# 设置环境变量

macOS / Linux：

```bash
export OPENAI_API_KEY="你的-api-key"
```

Windows PowerShell：

```powershell
$env:OPENAI_API_KEY="你的-api-key"
```

IntelliJ IDEA 里也可以在 Run Configuration 里设置：

```text
OPENAI_API_KEY=你的-api-key
```

不要把 API Key 提交到 Git。
