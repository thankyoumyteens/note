# 为什么要做流式输出？

普通接口是这样：

```text
用户请求
  ↓
等待模型完整生成
  ↓
一次性返回完整答案
```

问题是：模型生成长回答时，用户要等很久。

流式接口是这样：

```text
用户请求
  ↓
模型生成一点
  ↓
后端返回一点
  ↓
前端展示一点
```

优点：

```text
用户体感更快
适合长文本生成
适合聊天产品
适合 Agent 展示中间过程
```

后面做 RAG、Agent、MCP 工具调用时，流式输出也很重要。
