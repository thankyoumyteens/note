# 重连与恢复

SSE 原生 `EventSource` 支持自动重连和 `Last-Event-ID`，但它只适合 GET。

如果你是 POST SSE，通常前端会用：

```text
fetch + ReadableStream
```

这时自动重连要自己做。

## 是否需要断点续传

LLM 流式输出一般不建议做严格断点续传，因为模型生成不是天然可恢复的。

更现实的方案是：

- 短回答：断流后提示用户重试
- 长任务：使用 taskId 保存状态，让用户重新拉取结果
- Agent 任务：后台执行，前端只订阅进度

如果是重要任务，不建议完全依赖一次 SSE 长连接。
