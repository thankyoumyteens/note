# 第五步：攻克上下文管理 (打破“无状态”诅咒)

作为 Java 后端，你非常熟悉 JSESSIONID、HttpSession 或是挂载在 Redis 里的 Token。在传统 Web 开发中，服务器会帮你记住“这个客户端是谁，他刚才干了什么”。

但在 AI 原生开发中，大模型 API 就像一个重度失忆症患者。哪怕相隔只有一毫秒，第二次 HTTP 请求也完全不记得第一次聊过什么。你必须像“每次 HTTP 请求都把所有的 Cookie 和历史浏览记录全带上”一样，把整个聊天数组传过去。
