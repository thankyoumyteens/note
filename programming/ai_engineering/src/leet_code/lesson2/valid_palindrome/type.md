# 题型识别信号

题目特征：

```text
给一个字符串 s
判断它是否是回文
忽略大小写
忽略非字母数字字符
```

识别信号：

```text
palindrome
回文
从左往右和从右往左相同
忽略大小写
跳过非字母数字字符
```

看到这些信号，优先想到：

```text
双指针
```

原因是回文的本质是：

```text
左边字符 == 右边字符
```

所以天然适合：

```text
left 从左往右
right 从右往左
```
