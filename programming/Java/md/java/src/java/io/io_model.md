# IO 模型

常见的 IO 模型:

1. 同步阻塞 IO(Blocking IO)
2. 同步非阻塞 IO(Non-blocking IO)
3. IO 多路复用模型(IO Multiplexing)
4. 异步 IO(Asynchronous IO)

## 数据拷贝

每次读/写操作, 数据都要经历两次拷贝。

向磁盘/网卡写入数据的过程:

1. 把数据从用户的缓冲区中拷贝到内核的的缓冲区
2. 把内核缓冲区中的数据，拷贝到磁盘/网卡的缓冲区

从磁盘/网卡读取数据的过程:

1. 把磁盘/网卡中的数据拷贝到操作系统内核的缓冲区
2. 把内核缓冲区中的数据拷贝到用户的缓冲区
