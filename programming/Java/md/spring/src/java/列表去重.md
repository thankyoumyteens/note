# 列表去重

## 根据指定字段去重

```java
// 有重复数据
List<User> list = queryUserList();
// 根据id去重
Set<User> set = new TreeSet<>(Comparator.comparing(User::getId));
set.addAll(list);
// 去重后的列表
List<User> result = new ArrayList<>(set);
```
