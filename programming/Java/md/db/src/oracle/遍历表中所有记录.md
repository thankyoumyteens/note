# 遍历表中所有记录

```sql
-- 查询参数
declare query_time VARCHAR2(32);

begin

query_time := '2020-01-01';

for task_row in (select * from T_TASK_INFO where CREATE_TIME > query_time)
loop
    dbms_output.put_line('当前遍历到的任务: ' || task_row.TASK_NAME);
end loop;

end;
```
