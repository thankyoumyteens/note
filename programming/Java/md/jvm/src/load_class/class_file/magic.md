# 魔数

很多文件格式都会规定满足该格式的文件必须以某几个固定字节开头，这几个字节主要起标识作用，叫作魔数(Magic Number)​。例如 PDF 文件以 4 字节 `%PDF`​（0x25、0x50、0x44、0x46）开头，ZIP 文件以 2 字节 `PK`​（0x50、0x4B）开头。

每个 class 文件的头 4 个字节被作为魔数, 它的唯一作用是表示这个文件是一个 class 文件, 固定为 `0xCAFEBABE`。

![](../../img/class_file_magic.png)
