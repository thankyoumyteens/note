# 主从同步

二进制日志(Binary Logging)记录了主库所有的 DDL 语句和 DML 语句, 但不包括查询语句。

1. 主库在事务提交时, 会把数据的变更记录到二进制日志中
2. 从库上的 I/O 线程负责连接到主库并请求二进制日志, 然后将其存储在本地的中继日志(Relay Log)中
3. 从库上的 SQL 线程负责从本地的中继日志中读取事件, 并在从库上重放这些事件, 以确保与主库的数据保持一致
