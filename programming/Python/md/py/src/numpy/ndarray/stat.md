# 统计运算

- `sum` 求和
- `cumsum` 累加求和
- `mean` 求平均数
- `std` 求标准差
- `var` 求方差
- `min` 求最小值
- `max` 求最大值
- `argmin` 求最小值索引
- `argmax` 求最大值索引
- `sort` 排序

```py
import numpy as np

arr1 = np.array([1, 2, 3])

v = np.mean(arr1)

# 2.0
print(v)
```
