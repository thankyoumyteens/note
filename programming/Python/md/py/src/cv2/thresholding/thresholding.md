# 阈值处理

阈值处理（Thresholding）是将灰度图像转换为二值图像（仅含黑白两种颜色）的核心技术，通过设定 “阈值” 将像素值分为两类（高于阈值和低于阈值），广泛用于图像分割、边缘检测、目标提取等场景。

灰度图像的像素值范围是 0~255（0 为黑，255 为白）。阈值处理的逻辑是：

1. 设定一个阈值 thresh（如 127）；
2. 对每个像素值 pixel 与 thresh 比较；
3. 根据比较结果，将像素值赋值为 最大值（如 255，白色） 或 最小值（如 0，黑色），生成二值图像。
