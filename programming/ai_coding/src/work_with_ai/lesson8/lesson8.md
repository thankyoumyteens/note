# 第 8 课：轻量 Spec 结构

第 6～7A 课你已经学了：

```text
需求 → Plan → 小步实现 → diff 检查 → 工作流沉淀
```

第 8 课开始升级为：

```text
需求 → Spec → Plan → Tasks → Test → 实现
```

也就是用轻量规格文件管理一个稍微复杂一点的功能。

本课实战功能是：

```http
GET /api/documents/{id}
```

但注意：**第 8 课只写 Spec，不写代码。**
