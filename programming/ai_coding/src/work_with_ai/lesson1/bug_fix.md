# 如果 AI 创建项目失败，怎么让它修

比如 `mvn test` 失败，你不要直接说：

```text
报错了，修一下。
```

要这样说：

```text
mvn test 失败。请先分析失败原因，不要直接大改。

错误日志：
[粘贴完整错误日志]

要求：
1. 找出失败的直接原因
2. 说明涉及哪些文件
3. 给出最小修改方案
4. 修改后重新运行 mvn test
5. 不要加入新依赖，除非必须并说明原因
```

这会让 AI 按“调查 → 最小修改 → 验证”的方式处理问题。
