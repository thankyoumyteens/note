# 第 11 课：最小实现让测试通过

第 10 课你已经完成了 TDD 的 Red 阶段：

```text
先写 summary 接口测试
测试失败
失败原因应该是 POST /api/documents/{id}/summary 尚未实现
```

第 11 课要做的是 Green 阶段：

```text
写最小实现，让测试通过
```

核心原则：

```text
只实现测试要求的行为。
不要顺手重构。
不要扩大功能范围。
不要接真实 AI API。
```
