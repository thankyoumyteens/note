# 第六步：最终检查 diff

测试通过后，让 Codex 审查当前 diff：

```text
目标：
继续第 7 课。现在只执行最终检查，不要修改代码。

请审查当前 git diff，确认是否符合第 7 课要求。

重点检查：
1. 是否只实现 POST /api/documents。
2. 是否没有修改 pom.xml。
3. 是否没有新增依赖。
4. 是否没有接入数据库。
5. 是否没有接入 AI API。
6. 是否没有加入用户系统或 Spring Security。
7. 是否没有实现查询接口或摘要接口。
8. 成功响应是否只返回 documentId。
9. title/content 空值和纯空格校验是否覆盖。
10. 是否有测试覆盖成功和失败路径。
11. 是否有无关文件修改。

请按以下格式输出：
- 符合要求
- 需要修正
- 风险提示
- 建议提交信息

限制：
不要修改任何文件。
```
