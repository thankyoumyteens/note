# 第五步：运行测试

让 Codex 运行：

```text
目标：
继续第 7 课。现在只执行第 5 小步：运行测试并报告结果。

请运行：
mvn test

要求：
1. 如果测试通过，请总结结果。
2. 如果测试失败，请先分析失败原因。
3. 修复失败时只做最小修改。
4. 不要新增依赖。
5. 不要改变成功响应格式。
6. 不要修改无关文件。
```

或者你自己手动运行：

```bash
mvn test
```

如果失败，把完整错误日志发给 Codex，不要只说“报错了”。
