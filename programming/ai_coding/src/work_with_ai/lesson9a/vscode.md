# VS Code Extension

官方资料显示它有 VS Code 扩展，可以在 VS Code 侧边栏里查看 spec workflow 数据，包括项目统计、spec 管理、任务管理、审批流程、实时更新等能力。

安装方式：

```text
VS Code → Extensions → 搜索 Spec Workflow MCP
```

是否必须安装？

```text
不是必须。
```

你可以先只用 MCP + Dashboard。等你觉得 Dashboard 切换麻烦，再装 VS Code Extension。
